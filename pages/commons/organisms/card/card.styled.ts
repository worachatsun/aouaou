import { COLOR } from 'global/constants';

export const cardMedia = {
  height: 45,
  width: 45,
  border: `1px solid ${COLOR.GREY.PRIMARY}`,
  marginLeft: 10,
  borderRadius: 5,
};

export const cardContainer = {
  maxWidth: 214,
  borderRadius: 5,
  borderColor: `${COLOR.GREY.PRIMARY}`,
  margin: '24px 24px 0 0',
  maxHeight: '280px',
};
